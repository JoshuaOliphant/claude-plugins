# Per-project harness config — sourced by install.sh / start.sh / stop.sh / status.sh.
# Rendered at scaffold time. Edit and re-run start.sh to change mode.
SERVICE_NAME="{{SERVICE_NAME}}"
OBS_MODE="{{OBS_MODE}}"   # lite | full
